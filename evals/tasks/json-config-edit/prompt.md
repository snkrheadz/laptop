Add an MCP server named `context7` to the `mcpServers` section of
`settings.json` in this directory. It is a stdio server whose command is
`context7-mcp` (no args). Do not change anything else in the file.
